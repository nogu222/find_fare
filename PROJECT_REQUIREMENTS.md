# Business Fare Scanner — 프로젝트 요구사항

이 문서는 새 Codex 작업에서도 프로젝트 목적과 제한사항을 잃지 않기 위한 기준 문서입니다. 구현을 변경하기 전에 이 문서와 `IMPLEMENTATION_PLAN.md`를 먼저 읽습니다.

## 1. 목적

개인적으로 사용하는 V0.1 실험판을 만든다. 일반 Business 왕복보다 **하나의 Google Flights multi-city 검색으로 발권되는 전체 여정**이 더 저렴해지는 운임 가격 역전을 찾는다.

예시:

- 기준 왕복: `ICN → CDG / CDG → ICN`
- 비교 multi-city: `ICN → CDG / CDG → IST / IST → ICN`

Airwander처럼 서로 다른 별도 항공권 가격을 합치는 것이 아니다. Hidden-city 발권도 추천하지 않는다.

## 2. V0.1 입력

- 출발공항
- 목적공항
- 출국일
- 귀국일
- 추가로 시험할 도시 목록(사용자가 직접 입력, 최대 10개)
- 성인 1명
- 좌석등급은 Business 고정

기본 검증 조건은 다음과 같다.

- 출발: `ICN`
- 목적지: `CDG`
- 출국일: `2026-10-10`
- 귀국일: `2026-10-20`
- 추가도시 예시: `HEL`, `IST`, `MAD`, `FCO`, `LIS`
- 통화: `KRW`

## 3. 검색 및 비교 방식

1. `ICN → CDG / CDG → ICN` Business 왕복 최저가를 기준 가격으로 조회한다.
2. 추가도시 하나마다 하나의 multi-city 검색을 실행한다.
3. 예를 들어 IST는 `ICN → CDG / CDG → IST / IST → ICN` 전체 가격을 조회한다.
4. 기준 왕복과 통화가 같은 유효 결과만 비교한다.
5. 절감액은 `기준 가격 - multi-city 가격`으로 계산한다.
6. 절감률은 `(절감액 / 기준 가격) × 100`으로 계산한다.
7. 여러 도시 결과를 총 가격이 저렴한 순서대로 표시한다.
8. 기준보다 비싼 결과도 숨기지 않는다.

별도 발권 항공권의 가격을 합산해서 multi-city 가격처럼 표시하면 안 된다.

## 4. 결과 화면 필수 정보

- 기준 왕복 여정과 가격
- multi-city 여정
- 총 가격과 통화
- 기준 왕복 대비 가격 차이
- 절감액과 절감률
- 총 비행시간 또는 총 이동시간
- 항공사와 항공편 번호(응답에 있는 경우)
- 각 실제 flight segment의 travel class
- 전체 Business인지 Mixed Cabin인지
- 검색 성공/실패 여부
- 오류 종류
- 이번 검색에서 사용한 API 호출 횟수

가격이 기준보다 저렴한 결과는 눈에 잘 띄게 표시한다.

## 5. Cabin 판정

검색 요청을 Business로 보냈다는 이유만으로 모든 구간이 Business라고 추측하지 않는다. 실제 응답의 각 flight segment에서 travel class를 확인한다.

예시:

- `ICN → DOH`: Business
- `DOH → CDG`: Business
- `CDG → IST`: Economy
- `IST → ICN`: Business

Business가 아닌 구간이 하나라도 확인되면 `Mixed Cabin`으로 표시한다. 좌석등급 데이터가 없으면 Business라고 만들어내지 않고 `정보 없음`으로 표시한다.

## 6. SerpApi 원칙

- SerpApi Google Flights API를 사용한다.
- 현재 공식 문서에서 round trip, multi-city, Business cabin, 가격, 통화, total duration, flight segment travel class 및 필요한 token/추가 요청 규격을 확인한 뒤 구현한다.
- 공식 문서나 실제 응답에 없는 필드를 추측하지 않는다.
- API Key는 서버의 `SERPAPI_KEY` 환경변수에서만 읽는다.
- API Key를 코드, 로그, 브라우저 응답 또는 GitHub에 노출하지 않는다.
- Key 존재 여부만 확인할 때도 실제 값을 출력하지 않는다.
- 무료 호출량을 보호한다. 먼저 기준 왕복 1회, 다음으로 추가도시 1개만 검증한 후 확장한다.
- 같은 검색 조건은 가능한 범위에서 캐시한다.
- 사용자의 명시적 실행 없이 수십~수백 도시를 자동 검색하지 않는다.

## 7. 가격 및 오류 안전 원칙

- 가격이 없으면 `0원`이나 가짜 가격을 표시하지 않는다.
- 통화가 다르면 그대로 비교하지 않는다.
- 응답 구조가 예상과 다르면 실제 응답을 확인한 후 파서를 수정한다.
- 가능한 범위에서 다음 오류를 구별한다.
  - `KEY_MISSING`
  - `INVALID_INPUT`
  - `NO_RESULT`
  - `API_KEY_ERROR`
  - `API_LIMIT`
  - `API_ERROR`
  - 지원되지 않는 multi-city 구조

## 8. V0.1에서 하지 않는 것

- 회원가입, 사용자 계정
- 결제, 광고, 제휴마케팅
- 복잡한 데이터베이스
- 가격 알림
- 전 세계 공항 자동 탐색
- AI 추천
- 모바일 앱
- 불필요한 애니메이션
- 실제 항공권 예약 또는 결제
- 별도 발권 항공권 조합
- Hidden-city 발권 추천

## 9. 기술 및 운영 원칙

- 브라우저에서 사용하는 웹앱으로 만든다.
- API Key는 브라우저에 노출하지 않는다.
- 회사 PC에 별도 프로그램 설치를 요구하지 않는다.
- Codex 클라우드와 GitHub 저장소를 이용해 개발·실행·테스트한다.
- 코드 구조는 V0.1에 필요한 만큼만 단순하게 유지한다.
- 기능을 한꺼번에 만들지 않고 `IMPLEMENTATION_PLAN.md` 순서대로 한 단계씩 실제 동작을 확인한다.

## 10. 초보자 안내 원칙

사용자가 직접 해야 할 일이 있을 때만 요청한다. 개발용어만 나열하지 말고 다음을 한국어로 설명한다.

1. 어디를 클릭하는지
2. 무엇을 입력하는지
3. 무엇이 보이면 정상인지

가능한 코드 변경, 실행, 테스트, 커밋은 Codex가 직접 수행한다. API Key 값을 채팅에 붙여 넣도록 요구하지 않는다.

명령이나 외부 서비스 연결이 환경 제한 때문에 실패했을 때 `실패했다`고만 보고하지 않는다. 반드시 다음을 함께 설명한다.

1. 사용자의 입력이나 실수 때문인지, Codex 실행 환경의 제한 때문인지
2. 실패 전에 완료된 작업과 완료되지 않은 작업
3. API 호출량, 파일 또는 비용이 사용되었는지
4. 사용자가 다음에 눌러야 할 화면과 버튼
5. 입력해야 할 정확한 값 또는 문장
6. 완료되면 화면에 무엇이 보여야 정상인지
7. 사용자가 비밀 키, 명령어 또는 코드를 채팅에 붙여 넣지 않고 해결할 수 있는 방법

사용자에게 터미널 명령 실행이나 로컬 프로그램 설치를 먼저 요구하지 않는다. Codex/GitHub 웹 화면에서 해결할 수 있는 방법을 우선하여 한 단계씩 안내한다.

## 11. V0.1 성공 기준

1. `ICN → CDG → ICN` Business 왕복 가격을 실제로 조회한다.
2. `ICN → CDG / CDG → IST / IST → ICN` 같은 하나의 multi-city 가격을 실제로 조회한다.
3. 두 가격을 같은 통화에서 비교한다.
4. 더 저렴하면 절감액과 절감률을 정확히 계산한다.
5. 여러 추가도시 결과를 저렴한 순서대로 보여준다.
6. 실제 항공편 정보와 구간별 cabin을 확인할 수 있다.

## 12. 새 Codex 작업 시작 문장

동일 저장소와 최신 작업 브랜치를 연 뒤 다음 문장으로 시작할 수 있다.

> `PROJECT_REQUIREMENTS.md와 IMPLEMENTATION_PLAN.md를 먼저 읽고, 현재 구현 및 테스트 상태를 확인한 다음 다음 미완료 단계만 진행해줘. 실제 API 호출 전에는 조건과 예상 호출 횟수를 알려주고, SERPAPI_KEY 값은 절대 출력하지 마.`
