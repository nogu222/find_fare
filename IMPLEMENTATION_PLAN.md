# V0.1 구현 계획

각 단계가 실제로 동작하는지 확인한 뒤 다음 단계로 이동합니다.

1. **최소 프로젝트 구조** — Next.js로 브라우저 웹앱 기반을 만든다. (완료)
2. **기본 입력 화면** — 출발공항, 목적공항, 출국일, 귀국일, 추가 도시를 입력하고 형식을 검증한다. (완료)
3. **기준 왕복 1건** — SerpApi Business 왕복 최저가 한 건을 서버에서 조회한다. 키 누락, 호출 제한, 결과 없음 오류를 구분한다. (구현 완료, 실제 키로 응답 확인 필요)
4. **추가 도시 1개** — 하나의 발권 가능한 multi-city 요청(ICN→CDG, CDG→IST, IST→ICN)을 조회한다. 별도 티켓 가격을 합치지 않는다.
5. **가격 비교** — 통화가 같은 유효 결과만 비교해 절감액과 절감률을 계산하고 테스트한다.
6. **여러 도시 순차 검색** — 한 번에 최대 10개 도시를 순차 조회하고 같은 조건은 캐시해 호출 낭비를 줄인다. 호출 횟수를 화면에 표시한다.
7. **구간별 좌석 등급** — 실제 응답의 각 항공편 구간 travel class를 표시하고 Economy가 하나라도 있으면 Mixed Cabin으로 표시한다.
8. **결과 및 오류 화면** — 가격순 정렬, 총 이동시간, 성공/실패 상태와 No result/API error/입력 오류/API 제한/미지원 구조를 가능한 범위에서 구분한다.

## 기술 선택 이유

- **Next.js**: 화면과 비밀 API 키를 사용하는 서버 코드를 한 프로젝트에서 관리할 수 있어 구조가 단순합니다.
- **명시적인 응답 검증**: API 응답 구조가 달라졌을 때 잘못된 가격을 조용히 표시하지 않도록, 연동 단계에서 필수 값을 하나씩 검사합니다.
- **서버 전용 API 호출**: `SERPAPI_KEY`가 브라우저에 전달되지 않도록 합니다.
- **파일/메모리 기반의 단순 캐시부터 시작**: V0.1에는 복잡한 데이터베이스를 추가하지 않습니다.

## 단계별 검증 원칙

- 가격이 없으면 `0원`을 만들지 않고 결과 없음으로 처리합니다.
- 공식 문서에 없는 필드나 값을 추측하지 않습니다.
- 실제 응답 샘플에서 가격, 통화, 구간, 좌석 등급, 시간 필드를 확인한 뒤 파서를 작성합니다.
- 무료 호출량을 보호하기 위해 자동 대량 탐색과 동시에 많은 요청을 보내는 기능은 만들지 않습니다.
